# 📊 รายงานวิเคราะห์โค้ดเชิงลึก: OPTIMIZED ROUTING SCRIPT (PRO VERSION)

ผมอ่านโค้ดทั้งไฟล์ครบแล้ว (259 บรรทัด, Google Apps Script + Google Routes API v2) ขอสรุปแบบละเอียดทุกมิติตามที่ขอครับ

---

## [ 1 ] ภาพรวมการทำงานของระบบ (Flow ทั้งหมด)

โค้ดนี้เป็นระบบ **คำนวณเส้นทางขนส่งหลายจุดส่ง (Multi-stop Routing)** สำหรับงานเบิกส่วนต่างค่าขนส่ง SCG วังน้อย โดยทำงานร่วมกับ AppSheet/Google Sheets:

```
findOptimalRouteUsingExistingDistance(shipmentId, rowId)   ← Entry point (น่าจะถูกเรียกจาก AppSheet Bot)
    │
    ├─ 1️⃣ prepareWaypointsWithExistingDistance()
    │      • เปิดชีต "SCGนครหลวงJWDภูมิภาค"
    │      • ใช้ TextFinder หาแถวที่ Shipment No ตรงกัน (matchEntireCell)
    │      • Parse พิกัด Lat,Lng จากคอลัมน์ "จุดส่งสินค้าปลายทาง" (ล้างอักขระขยะด้วย regex)
    │      • ดึงระยะทางจากคลัง (คอลัมน์ "ระยะทางจากคลัง_Km") + ชื่อปลายทาง
    │      • ใส่คลังวังน้อย (Depot) เป็นจุดแรกเสมอ
    │
    ├─ 2️⃣ selectFinalDestinationAndSort()
    │      • เรียงปลายทางตามระยะจากคลัง มาก→น้อย
    │      • เลือก "จุดไกลสุด" เป็นปลายทางสุดท้าย ที่เหลือเป็น intermediates
    │
    ├─ 3️⃣ executeGoogleMapsRoutesAPIOneWay()
    │      • ยิง POST → routes.googleapis.com/directions/v2:computeRoutes
    │      • travelMode: DRIVE, routingPreference: TRAFFIC_UNAWARE (ผลคงที่ ตรวจสอบซ้ำได้ = เหมาะกับงานเบิกเงิน ✅)
    │      • optimizeWaypointOrder: true (ถ้ามีจุดกลาง >1) ให้ Google จัดลำดับจุดกลางให้
    │      • คำนวณระยะรวม (กม.) + สร้างลิงก์ google.com/maps/dir/...
    │
    └─ 4️⃣ writeResultsToSheet()
           • หาแถวในชีต "ทำเบิกส่วนต่างScgวังน้อย" ด้วย TextFinder จาก rowId
           • เขียน: string พิกัดเรียงลำดับ | ระยะทาง Km | ลิงก์แผนที่
           • เขียนพิกัดแยกช่อง Lat/Long_ปลายทาง_01...20 แบบ Batch (setValues ครั้งเดียว)
```

**จุดที่ออกแบบมาดี (ขอชมก่อน):**
- ✅ ใช้ **Routes API v2** (ตัวใหม่) แทน Directions API (Legacy) — ถูกทางแล้ว
- ✅ `X-Goog-FieldMask` จำกัด field ที่ขอ → ลดค่าใช้จ่าย/latency ตาม best practice ของ Google
- ✅ `TRAFFIC_UNAWARE` → ผลลัพธ์ deterministic เหมาะกับงานเบิกเงินที่ต้อง audit ซ้ำได้
- ✅ API Key เก็บใน Script Properties ไม่ hardcode
- ✅ `muteHttpExceptions: true` แล้วเช็ค response code เอง
- ✅ Batch write 20 คอลัมน์ครั้งเดียว (`setValues`) เร็วกว่าเขียนทีละเซลล์ 20 รอบมาก

---

## [ 2 ] 🐛 บั๊กจริง (Bugs) — ควรแก้ทันที

### 🔴 Bug #1: Range เกินขอบชีต (Off-by-one) — เจอ 2 จุด
```js
// บรรทัด 81
const searchRange = computedSheet.getRange(2, shipmentColIdx, computedSheet.getLastRow(), 1);
// บรรทัด 230
const idRange = resultSheet.getRange(2, idIndex + 1, resultSheet.getLastRow(), 1);
```
เริ่มที่แถว 2 แต่ขอจำนวนแถว = `getLastRow()` → range ยาวเกินจริง **1 แถว** ถ้าข้อมูลเต็มจนถึงแถวสุดท้ายของ grid (`getLastRow() == getMaxRows()`) จะ **throw "out of bounds" ทันที** และถ้าชีตมีแค่ header (`getLastRow()=1`) จะไปค้นแถวว่างโดยไม่รู้ตัว

**แก้เป็น:**
```js
const numRows = Math.max(computedSheet.getLastRow() - 1, 1);
const searchRange = computedSheet.getRange(2, shipmentColIdx, numRows, 1);
```

### 🔴 Bug #2: Batch write 20 คอลัมน์ "เขียนทับแบบมั่นใจเกินไป"
```js
resultSheet.getRange(rowIndexInSheet, firstDestColIndex + 1, 1, maxColumns).setValues([valuesToWrite]);
```
โค้ดหา **แค่คอลัมน์แรก** (`Lat/Long_ปลายทาง_01`) แล้วเขียนทับ 20 คอลัมน์ถัดไป **โดยไม่ตรวจว่าคอลัมน์เหล่านั้นคือ _02..._20 จริงหรือไม่** ถ้าวันหนึ่งมีใครแทรกคอลัมน์อื่นคั่นกลาง หรือชีตมีช่องปลายทางไม่ครบ 20 → **ข้อมูลคอลัมน์อื่นจะโดนล้างเป็นค่าว่างเงียบๆ** อันตรายมากสำหรับชีตเบิกเงิน

**แก้:** ตรวจ header ทั้ง 20 ช่องก่อนเขียน:
```js
const destColIndexes = [];
for (let i = 1; i <= 20; i++) {
  const idx = resultHeader.indexOf(`Lat/Long_ปลายทาง_${String(i).padStart(2, '0')}`);
  destColIndexes.push(idx);
}
// เขียนเฉพาะช่องที่มีอยู่จริง
```

### 🔴 Bug #3: `routes[0]` อาจระเบิดแบบงงๆ
```js
const route = result.routes[0];
```
Routes API สามารถตอบ **HTTP 200 พร้อม body ว่าง `{}`** ได้ (เช่นหาเส้นทางไม่เจอ/พิกัดอยู่กลางทะเล) → จะได้ `TypeError: Cannot read property '0' of undefined` ซึ่ง debug ยากมาก

**แก้:**
```js
if (!result.routes || result.routes.length === 0) {
  throw new Error(`Routes API returned no route. Response: ${response.getContentText().substring(0, 500)}`);
}
```

---

## [ 3 ] 🤫 Error เงียบ (Silent Failures) — มีจริง 6 จุด

นี่คือส่วนที่คุณถามเจาะจง และเป็นจุดอ่อนหลักของโค้ดชุดนี้:

| # | จุด | พฤติกรรมเงียบ | ความเสี่ยง |
|---|------|----------------|-----------|
| 1 | บรรทัด 103, 107-113 | พิกัดว่าง/parse ไม่ได้ → `return` ข้ามเงียบๆ **ไม่มี log ไม่มี warning** | Shipment มี 5 จุดส่ง แต่พิกัดเสีย 2 จุด → คำนวณแค่ 3 จุด **ระยะทางเบิกน้อยกว่าจริง** โดยไม่มีใครรู้ 🚨 |
| 2 | บรรทัด 106 regex `/[^\d.,-]/g` | เซลล์ที่มีตัวเลขปนข้อความ เช่น `"บ้านเลขที่ 99/1 ต.ลำไทร"` → ล้างแล้วเหลือ `99.1,...` อาจ parse ได้ **พิกัดผิดๆ** โดยไม่ error | ได้เส้นทางเพี้ยนแบบตรวจไม่เจอ |
| 3 | บรรทัด 115 `parseFloat(...) || 0` | ระยะจากคลัง parse ไม่ได้ → กลายเป็น 0 เงียบๆ | จุดนั้นจะไม่มีวันถูกเลือกเป็นปลายทางสุดท้าย ทั้งที่อาจไกลสุดจริง → ลำดับเส้นทางผิด |
| 4 | บรรทัด 200 `if (intermediates[index])` | ถ้า index จาก Google เกินขอบ → **จุดส่งหายจากผลลัพธ์เงียบๆ** แต่ `totalDistance` จาก API ยังรวมจุดนั้นอยู่ | String พิกัด/ลิงก์ ไม่ตรงกับระยะทางที่เบิก → audit ไม่ผ่าน |
| 5 | บรรทัด 250-252 | ปลายทางเกิน 20 จุด → จุดที่ 21+ **หายจากช่องแยกเงียบๆ** (ยังอยู่ใน string หลัก) | ข้อมูล 2 ที่ไม่ตรงกัน |
| 6 | `distanceColIdx === 0` (คอลัมน์ระยะหาย) | ทุกจุด distance = 0 → sort ไม่เปลี่ยนอะไร → จุดแรกที่เจอกลายเป็น "ปลายทางสุดท้าย" แบบสุ่ม | ไม่มี error แจ้งว่าคอลัมน์หาย |

**คำแนะนำรวม:** เพิ่มระบบ **เก็บ warnings แล้วเขียนลงคอลัมน์ "สถานะ/หมายเหตุ" ในชีตผลลัพธ์** เช่น:
```js
const warnings = [];
if (!latlngRaw) { warnings.push(`แถว ${rowIdx}: พิกัดว่าง`); return; }
// ... ตอนจบเขียน warnings.join("; ") ลงชีต
```
และ **ตรวจช่วงพิกัดประเทศไทย** ป้องกันข้อ 2:
```js
const isValidThaiCoord = (lat, lng) => lat >= 5 && lat <= 21 && lng >= 97 && lng <= 106;
```

---

## [ 4 ] ⚡ ประสิทธิภาพ (Performance) — มีจุดคอขวดใหญ่ 1 จุด

### 🟠 คอขวดหลัก: อ่านเซลล์ทีละช่องในลูป (บรรทัด 99-133)
โค้ดคอมเมนต์ว่า "เตรียมข้อมูลแบบจรวด" แต่จริงๆ **ในลูปมีการเรียก `getRange().getValue()` ถึง 3 ครั้งต่อแถว** (พิกัด + ระยะทาง + ชื่อ) แต่ละครั้งคือ 1 round-trip ไป Sheets API:

- Shipment 15 จุดส่ง = **45 API calls** ≈ หลายวินาที
- TextFinder ช่วยตอน "ค้นหา" จริง แต่ประโยชน์ถูกกลืนหายตอน "อ่านข้อมูล"

**แก้: อ่านทั้งแถวครั้งเดียวต่อ match (ลดเหลือ 1 call/แถว) หรือดีกว่านั้นคือรวบเป็น RangeList:**
```js
matches.forEach(match => {
  const rowIdx = match.getRow();
  // อ่านทั้งแถวครั้งเดียว แล้วหยิบตาม index
  const rowValues = computedSheet.getRange(rowIdx, 1, 1, header.length).getValues()[0];
  const latlngRaw = rowValues[latlngColIdx - 1];
  const distance  = parseFloat(rowValues[distanceColIdx - 1]) || 0;
  const rawName   = rowValues[nameColIdx - 1];
  ...
});
```
เร็วขึ้น ~3 เท่าทันที และถ้าแถวที่ match อยู่ติดกัน (มักเป็นแบบนั้น) อ่าน block เดียวคลุมทั้งหมดได้เร็วสุด

### 🟡 จุดย่อยอื่นๆ
- **FieldMask ขอ `routes.duration` แต่ไม่เคยใช้** → ลบออก หรือนำมาใช้บันทึก "เวลาเดินทางโดยประมาณ" ลงชีตเพิ่ม (มีประโยชน์ต่องานจริง)
- **ไม่มี retry** — ถ้า Google ตอบ 429/500/503 ชั่วคราว งานล้มทันที ควรมี exponential backoff:
```js
function fetchWithRetry(url, options, maxRetries = 3) {
  for (let i = 0; i < maxRetries; i++) {
    const res = UrlFetchApp.fetch(url, options);
    const code = res.getResponseCode();
    if (code === 200) return res;
    if (code !== 429 && code < 500) throw new Error(`API Error ${code}: ${res.getContentText()}`);
    Utilities.sleep(Math.pow(2, i) * 1000);
  }
  throw new Error("API failed after retries");
}
```
- ถ้า AppSheet อาจยิงพร้อมกันหลายแถว → พิจารณา `LockService` กันเขียนชนกัน

---

## [ 5 ] 🔁 ความซ้ำซ้อน (Redundancy)

1. **Logic แยก origin/destination/intermediates ทำ 2 รอบ** — `selectFinalDestinationAndSort()` แยกไว้แล้ว (และ return `finalDestination, intermediates` ออกมา **แต่ caller ไม่เคยใช้**) จากนั้น `executeGoogleMapsRoutesAPIOneWay()` มา slice แยกใหม่อีกรอบจาก array เดิม → ควรส่ง object `{depot, intermediates, finalDestination}` ตรงๆ ครั้งเดียว
2. **การ sort intermediates ไร้ประโยชน์** — sort มาก→น้อยทั้ง array แต่เป้าหมายจริงคือแค่ "หาจุดไกลสุด" เพราะลำดับ intermediates เดี๋ยว Google `optimizeWaypointOrder` จัดใหม่ทั้งหมดอยู่ดี → ใช้ `reduce` หาค่า max พอ (สื่อเจตนาชัดกว่า):
```js
const finalDestination = destinations.reduce((max, p) => (p.distance > max.distance ? p : max), destinations[0]);
```
3. **Format พิกัด `toFixed(6)` ซ้ำ 3 ที่** (บรรทัด 208, 237, 251) → รวมเป็น helper:
```js
const fmtCoord = (p) => `${p.original.lat.toFixed(6)},${p.original.lng.toFixed(6)}`;
```
4. **`encodeURI` ไม่ได้ทำอะไรเลย** (บรรทัด 210) — URL ประกอบด้วยตัวเลข/จุด/คอมม่า/สแลชล้วนๆ ไม่มีอักขระให้ encode คอมเมนต์ "ป้องกัน URL ล้นทะลุ" จึงชวนเข้าใจผิด → ลบได้ (ไม่มีผลเสีย แต่รกโค้ด)

---

## [ 6 ] ⚠️ ข้อจำกัด/ความเสี่ยงเชิง Logic ที่ควรรู้

1. **ลิงก์ `google.com/maps/dir/` รองรับจุดแวะจำกัด** — บนเว็บ/มือถือ Google Maps แสดงได้ราวๆ **10 จุด** ถ้า shipment มี 15-20 จุด ลิงก์อาจเปิดแล้วจุดหาย/error → ควรเช็คและแจ้งเตือนในชีต หรือแตกลิงก์เป็นหลายท่อน
2. **Routes API จำกัด intermediates สูงสุด 25 จุด** — ถ้าเกินจะได้ API Error (อันนี้ไม่เงียบ แต่ควรดัก error message ให้อ่านง่าย)
3. **Heuristic "ไกลสุด = ปลายทางสุดท้าย"** สมเหตุสมผลสำหรับงานเบิก แต่ระวังว่าค่าในคอลัมน์ `ระยะทางจากคลัง_Km` ต้องน่าเชื่อถือ (ดูข้อ Silent #3)
4. **พิกัดซ้ำ** — ถ้ามีปลายทางพิกัดเดียวกัน 2 แถว (ลูกค้า 2 รายที่เดียวกัน) จะถูกส่งเข้า API ซ้ำ เปลือง waypoint slot → ควร dedupe ก่อน (แต่เก็บชื่อรวมกันไว้)
5. **`ss.getSheetByName(...)` ไม่เช็ค null** — ถ้าชื่อชีตถูกเปลี่ยน จะได้ error `Cannot read property 'getRange' of null` ที่งง → เพิ่ม `if (!sheet) throw new Error("ไม่พบชีต: ...")`
6. **เมื่อ error, ชีตไม่รู้อะไรเลย** — main function throw กลับไปที่ AppSheet เฉยๆ ควรเขียนสถานะ "❌ Error: ..." ลงคอลัมน์สถานะของแถวนั้น เพื่อให้คนทำเบิกเห็นในชีตทันที
7. **ค่าใช้จ่าย API**: การเปิด `optimizeWaypointOrder` + มี intermediates ทำให้ถูกคิดเงินระดับ **Routes: Compute Routes Advanced SKU** (แพงกว่า Basic) — ถูกต้องแล้วสำหรับงานนี้ แต่ควรรู้ไว้ + พิจารณา cache ผลด้วย `CacheService` กันยิงซ้ำ shipment เดิม

---

## [ 7 ] 🆕 ฟีเจอร์ใหม่ของ Google Maps Platform ที่แนะนำ (เกี่ยวข้องกับงานนี้โดยตรง)

| ฟีเจอร์ | ใช้ทำอะไร | เหมาะกับงานคุณเพราะ |
|---------|-----------|---------------------|
| **`routes.legs.distanceMeters`** (เพิ่มใน FieldMask) | ได้ระยะทาง **รายช่วง** (คลัง→จุด1, จุด1→จุด2, ...) | ใส่ระยะแยกรายจุดส่งลงชีตได้ → หลักฐานเบิกละเอียดขึ้นมาก แนะนำสุด ✨ |
| **Toll Info** (`extraComputations: ["TOLLS"]` + `routes.travelAdvisory.tollInfo`) | ประมาณ **ค่าทางด่วน** ของเส้นทาง | งานเบิกส่วนต่างค่าขนส่ง — เบิกค่าทางด่วนได้ด้วยตัวเลขจาก Google โดยตรง 💰 |
| **Fuel Consumption** (`extraComputations: ["FUEL_CONSUMPTION_ESTIMATION"]` + ระบุ `vehicleInfo.emissionType`) | ประมาณการใช้เชื้อเพลิง (ไมโครลิตร) | คำนวณค่าน้ำมันประกอบการเบิกได้ |
| **`routes.polyline`** + Static Maps API | ฝัง **ภาพแผนที่เส้นทางจริง** ลงชีต/PDF เบิกเงิน | หลักฐานเป็นภาพ ไม่ต้องพึ่งลิงก์ที่จำกัด 10 จุด |
| **Route Optimization API (GMPRO)** | จัดเส้นทางระดับ fleet: หลายคัน, กรอบเวลา, น้ำหนักบรรทุก | ถ้าอนาคตต้องจัดรถหลายคัน/วัน เกิน 25 จุด — ตัวนี้คือ upgrade path |
| **`computeRouteMatrix`** | ระยะทางแบบ matrix ทีเดียวหลายคู่ | ใช้แทน/ตรวจสอบคอลัมน์ `ระยะทางจากคลัง_Km` ให้เป็น **ระยะถนนจริง** แทนค่าที่คำนวณไว้เดิม |
| **Maps URLs API แบบ official** (`?api=1&origin=...&waypoints=...`) | ลิงก์แผนที่รูปแบบมาตรฐานที่ Google การันตี | เสถียรกว่ารูปแบบ `/dir/lat,lng/...` |
| `routeModifiers.avoidTolls` / `TWO_WHEELER` mode | เลี่ยงทางด่วน / โหมดมอเตอร์ไซค์ | ถ้านโยบายเบิกกำหนดว่าห้ามคิดเส้นทางด่วน |

**หมายเหตุ:** Directions API / Distance Matrix API ตัวเก่าเป็น **Legacy แล้ว** — โค้ดคุณใช้ Routes API v2 อยู่แล้วถือว่าอยู่บนเทคโนโลยีปัจจุบันถูกต้อง ✅

---

## [ 8 ] 📋 สรุปคะแนน + ลำดับความสำคัญที่ควรทำ

**คะแนนรวม: 7/10** — สถาปัตยกรรมดี ใช้ API รุ่นใหม่ถูกต้อง แต่มี bug ขอบเขต range, error เงียบหลายจุดที่กระทบความถูกต้องของ "เงินเบิก" โดยตรง และคอขวด performance ที่ขัดกับชื่อ "PRO"

| ลำดับ | สิ่งที่ควรทำ | เหตุผล |
|-------|-------------|--------|
| 🔴 ด่วน 1 | แก้ off-by-one ของ `getRange(2, ..., getLastRow(), 1)` ทั้ง 2 จุด | บั๊กจริง อาจ crash |
| 🔴 ด่วน 2 | เพิ่มระบบ warning เมื่อพิกัดเสีย/ถูกข้าม + ตรวจช่วงพิกัดไทย | ป้องกัน "เบิกขาด" แบบเงียบ |
| 🔴 ด่วน 3 | ตรวจ header ครบ 20 ช่องก่อน batch write | ป้องกันล้างข้อมูลคอลัมน์อื่น |
| 🟠 4 | อ่านข้อมูลทั้งแถวครั้งเดียวแทน 3 calls/แถว | เร็วขึ้น ~3 เท่า |
| 🟠 5 | เช็ค `routes` ว่าง + retry with backoff + เขียน error ลงชีต | ความทนทาน (resilience) |
| 🟡 6 | เพิ่ม `routes.legs.distanceMeters` + Toll Info ใน FieldMask | เพิ่มคุณค่าให้เอกสารเบิกทันที |
| 🟡 7 | รวม helper format พิกัด, ลบ sort/encodeURI ที่ไม่จำเป็น, dedupe พิกัดซ้ำ | ความสะอาดของโค้ด |
| 🟢 8 | Cache ผลด้วย CacheService (key = shipmentId) กันยิง API ซ้ำ | ประหยัดค่า API |

---

หากต้องการ ผมสามารถ **เขียนโค้ดเวอร์ชันปรับปรุง (PRO v2)** ที่แก้ทุกข้อข้างบนให้เลย — รวม legs distance รายช่วง, ค่าทางด่วน, ระบบ warning ลงชีต, retry, และแก้บั๊กทั้งหมด พร้อม commit + เปิด Pull Request ให้ตามขั้นตอนครับ สนใจให้ทำต่อเลยไหมครับ? 🚀