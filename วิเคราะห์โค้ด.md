## วิเคราะห์โค้ด OPTIMIZED ROUTING SCRIPT (PRO VERSION)

โค้ดนี้เป็น **Google Apps Script สำหรับจัดลำดับจุดส่ง + คำนวณระยะทางจริงจาก Google Routes API**  
ใช้กับงานเบิกส่วนต่างค่าขนส่ง SCG/JWD วังน้อย

---

## 1) บทบาทของระบบนี้

| หัวข้อ | รายละเอียด |
|--------|------------|
| **เป้าหมาย** | หาเส้นทางส่งสินค้า 1 Shipment (หลายจุด) แบบ one-way จากคลัง → จุดส่ง… → จุดไกลสุด |
| **อินพุต** | `shipmentId` + `rowId` (แถวในชีตผลลัพธ์) |
| **เอาต์พุต** | ลำดับพิกัด, ระยะทางรวม (กม.), ลิงก์ Google Maps, แยกพิกัดปลายทาง 01–20 |
| **โหมดจราจร** | `TRAFFIC_UNAWARE` — ระยะทางคงที่ ไม่ผันตามรถติด (เหมาะทำใบเบิก) |

**ไม่ใช่** ระบบ Master Data / แมชต์ร้าน  
**เป็น** ระบบคำนวณเส้นทางหลังมีพิกัดปลายทางแล้ว

---

## 2) ภาพรวมการทำงาน (Flow)

```
findOptimalRouteUsingExistingDistance(shipmentId, rowId)
        │
        ├─ 1) prepareWaypointsWithExistingDistance()
        │      • เปิดชีต SCGนครหลวงJWDภูมิภาค
        │      • TextFinder หาแถวที่ Shipment No ตรง
        │      • อ่านพิกัด + ระยะทางจากคลัง
        │      • ใส่ Depot เป็นจุดที่ 0 เสมอ
        │
        ├─ 2) selectFinalDestinationAndSort()
        │      • เรียงจุดส่งตามระยะจากคลัง (ไกล → ใกล้)
        │      • จุดไกลสุด = ปลายทางสุดท้าย
        │      • จุดอื่น = intermediates
        │
        ├─ 3) executeGoogleMapsRoutesAPIOneWay()
        │      • ยิง Routes API v2
        │      • optimizeWaypointOrder (ถ้า intermediate > 1)
        │      • ได้ระยะทาง + ลำดับที่ Google จัดใหม่
        │
        └─ 4) writeResultsToSheet()
               • หาแถวในชีต "ทำเบิกส่วนต่างScgวังน้อย" ด้วย rowId
               • เขียนผล 4 กลุ่มคอลัมน์
```

---

## 3) Configuration (ส่วนที่ 1)

```javascript
SPREADSHEET_ID     = "1CYtLpXn6gNYgbGu3oRF8CW5KkGYHQJ6D4jl9u2LiR6o"
SHEET_COMPUTED     = "SCGนครหลวงJWDภูมิภาค"      // แหล่งพิกัด
SHEET_RESULT       = "ทำเบิกส่วนต่างScgวังน้อย"   // ที่เขียนผล

DEPOT_COORDS = {
  lat: 14.1646106,
  lng: 100.6254644,
  name: "คลังสินค้า เอสซีจี เจดับเบิ้ลยูดี วังน้อย"
}
```

| ค่า | ความหมาย |
|-----|----------|
| `RESULT_COLUMN_NAME` | ลำดับพิกัดแบบ string: `lat, lng \| lat, lng \| ...` |
| `DISTANCE_COLUMN_NAME` | ระยะทางรวมจาก API (กม.) |
| `LINK_COLUMN_NAME` | ลิงก์ `https://www.google.com/maps/dir/...` |
| `DEPOT_DISTANCE_COLUMN` | คอลัมน์ `ระยะทางจากคลัง_Km` ใน SOURCE — ใช้เรียงจุด **ก่อน** ยิง API |

จุดสำคัญ: ลำดับเบื้องต้นอิง **ระยะจากคลังที่มีอยู่แล้วในชีต** ไม่ได้คำนวณใหม่เองในขั้นเตรียมข้อมูล

---

## 4) ฟังก์ชันหลักทีละตัว

### 4.1 `findOptimalRouteUsingExistingDistance(shipmentId, rowId)`

- Entry point ที่ระบบภายนอก (เช่น AppSheet / ปุ่ม / trigger) เรียก
- รับ 2 พารามิเตอร์:
  - `shipmentId` → ไปค้นในชีต SOURCE
  - `rowId` → ไปหาแถวที่จะเขียนในชีตผลลัพธ์
- ถ้า waypoint ≤ 1 จุด (มีแค่ depot) → error
- คืน object `{ Status, CalculatedDistanceKm, GoogleMapsLink }`
- error ถูก log แล้ว throw ต่อ (caller ต้องจับเอง)

---

### 4.2 `prepareWaypointsWithExistingDistance(ss, shipmentId)`

**วิธีอ่านข้อมูล (จุดที่เรียกว่า PRO):**

1. อ่าน header แถวเดียว
2. หา index คอลัมน์:
   - `Shipment No`
   - `จุดส่งสินค้าปลายทาง` (พิกัด)
   - `ชื่อปลายทาง`
   - `ระยะทางจากคลัง_Km`
3. ใช้ **TextFinder เฉพาะคอลัมน์ Shipment** → `matchEntireCell(true).findAll()`
4. วนเฉพาะแถวที่เจอ — **ไม่โหลดทั้งชีต**

**โครงสร้างจุดแต่ละจุด:**

```javascript
{
  id, name,
  original: { lat, lng },           // ใช้สร้างลิงก์ / เขียนชีต
  forApi: { location: { latLng: { latitude, longitude } } },  // รูปแบบ Routes API
  distance,   // จากคอลัมน์ระยะทางจากคลัง
  isDepot
}
```

**การทำความสะอาดพิกัด:**

```javascript
.replace(/[^\d.,-]/g, "")  // เหลือแค่ตัวเลข จุด ทศนิยม เครื่องหมายลบ คอมมา
.split(",")
```

ถ้าพิกัดว่าง / parse ไม่ได้ → ข้ามแถวนั้นเงียบ ๆ

**Depot** ถูก push เป็น `allPoints[0]` เสมอ (`distance: 0`, `isDepot: true`)

---

### 4.3 `selectFinalDestinationAndSort(allPoints)`

```
จุดส่งทั้งหมด (ไม่นับ depot)
  → เรียง distance มาก → น้อย
  → ตัวแรกหลังเรียง = finalDestination (ไกลสุดจากคลัง)
  → ที่เหลือ = intermediates
  → ลำดับส่งเข้า API: [depot, ...intermediates, finalDestination]
```

**นัยสำคัญ**
- “ปลายทางสุดท้าย” = จุดที่ **ไกลจากคลังที่สุด** ตามค่าในชีต ไม่ใช่จุดที่ Google เลือก
- ถ้า `ระยะทางจากคลัง_Km` ว่าง/ผิด ลำดับจะเพี้ยนทันที
- ถ้ามีจุดเดียว: intermediates ว่าง, final = จุดนั้น → ไม่ optimize

---

### 4.4 `executeGoogleMapsRoutesAPIOneWay(orderedPoints)`

| รายการ | ค่า |
|--------|-----|
| API | `https://routes.googleapis.com/directions/v2:computeRoutes` |
| Key | `Script Properties` → `GOOGLE_MAPS_API_KEY` |
| Mode | `DRIVE` |
| Routing | `TRAFFIC_UNAWARE` |
| Optimize | เปิดเมื่อ `intermediates.length > 1` |

**Field mask ที่ขอ:**
- `routes.duration`
- `routes.distanceMeters`
- `routes.optimizedIntermediateWaypointIndex`

**หลังได้ response**
1. ระยะทาง = `distanceMeters / 1000`
2. ถ้ามี optimize index → เรียง intermediates ตามที่ Google แนะนำ
3. ต่อ origin หน้า + destination ท้าย
4. สร้างลิงก์:
   ```
   https://www.google.com/maps/dir/lat,lng/lat,lng/...
   ```
   แล้ว `encodeURI` ทั้งก้อน

**สิ่งที่ API ไม่ได้ใช้**
- เวลาเดินทาง (`duration`) ถูกขอแต่ไม่ถูกเขียนลงชีต
- ไม่มี `TRAFFIC_AWARE` / เวลาจริง
- ไม่มีเส้นกลับคลัง (one-way จบที่จุดไกลสุด)

---

### 4.5 `writeResultsToSheet(resultSheet, rowId, ...)`

1. หาคอลัมน์จาก header
2. TextFinder หา `ID_ทำเบิกส่วนต่างScgวังน้อย` = `rowId`
3. เขียนทีละส่วน:

| คอลัมน์ | รูปแบบที่เขียน |
|---------|----------------|
| `GoogleMapsRoutesAPI` | `14.123456, 100.123456 \| 14.234567, 100.234567 \| ...` |
| `ระยะทาง_GoogleMapAPI_Km` | ตัวเลขกม. |
| `แสดงแผนที่_GoogleMapsRoutesAPI` | URL maps/dir |
| `Lat/Long_ปลายทาง_01` … `_20` | พิกัดลูกค้าเท่านั้น (ไม่รวม depot) สูงสุด 20 จุด |

**จุดออกแบบ**
- เขียนทีละเซลล์/เป็น batch 20 ช่อง — กันทับสูตรคอลัมน์อื่นของชีตเบิก
- ถ้าจุด > 20 จะถูกตัดเหลือ 20 (ไม่ error)

---

## 5) รูปแบบข้อมูลเข้า–ออก

### อินพุตที่ต้องมีในชีต SOURCE (`SCGนครหลวงJWDภูมิภาค`)

| คอลัมน์ | จำเป็น? | ใช้ทำอะไร |
|---------|---------|-----------|
| `Shipment No` | จำเป็น | ค้นแถว |
| `จุดส่งสินค้าปลายทาง` | จำเป็น | พิกัด `lat,lng` |
| `ชื่อปลายทาง` | ไม่บังคับ | ตั้งชื่อจุดใน log |
| `ระยะทางจากคลัง_Km` | สำคัญมาก | เลือกปลายทางสุดท้าย + เรียงต้นทาง |

### เอาต์พุตในชีต RESULT (`ทำเบิกส่วนต่างScgวังน้อย`)

| คอลัมน์ | เนื้อหา |
|---------|---------|
| `ID_ทำเบิกส่วนต่างScgวังน้อย` | key หาแถว |
| `GoogleMapsRoutesAPI` | ลำดับพิกัดทั้งเส้น (รวม depot) |
| `ระยะทาง_GoogleMapAPI_Km` | กม. จาก API |
| `แสดงแผนที่_GoogleMapsRoutesAPI` | ลิงก์เปิดแผนที่ |
| `Lat/Long_ปลายทาง_01`–`20` | พิกัดลูกค้าตามลำดับหลัง optimize |

---

## 6) จุดแข็งของโค้ดชุดนี้

1. **TextFinder เฉพาะคอลัมน์** — เร็วกว่า `getDataRange()` ทั้งชีตมาก เมื่อชีตใหญ่  
2. **Batch เขียนปลายทาง 20 ช่อง** — ลดครั้งเรียก API Sheets  
3. **TRAFFIC_UNAWARE** — ระยะทางนิ่ง เหมาะทำเอกสารเบิก  
4. **optimizeWaypointOrder** — ให้ Google จัดจุดกลางเมื่อมี ≥ 2 จุดกลาง  
5. **ไม่เขียนทับทั้งแถว** — ปลอดภัยต่อสูตรในชีตเบิก  
6. **Depot fixed** — จุดเริ่มต้นชัดเจนเสมอ  

---

## 7) ความเสี่ยง / ข้อจำกัด (สำคัญ)

| # | ประเด็น | ผลกระทบ |
|---|---------|---------|
| 1 | พึ่ง `ระยะทางจากคลัง_Km` ในชีต | ถ้าค่านี้ว่าง/ผิด → เลือกปลายทางสุดท้ายผิด |
| 2 | ไม่มี path กลับคลัง | ระยะทาง = ขาไปอย่างเดียว ไม่ใช่รอบเที่ยว |
| 3 | จุดส่ง > 20 | ถูกตัดในคอลัมน์แยก แต่ string หลักยังครบ |
| 4 | พิกัด parse ไม่ได้ | ข้ามเงียบ → อาจเหลือจุดน้อยกว่าจริง |
| 5 | API key ใน Script Properties | ไม่มี key = ล้มทั้งฟังก์ชัน |
| 6 | `openById` แข็ง | ใช้ได้เฉพาะสเปรดชีตนี้ ย้ายไฟล์ต้องแก้โค้ด |
| 7 | ไม่มี LockService | ถ้ารันพร้อมกันหลาย shipment อาจ race ตอนเขียน |
| 8 | ไม่ตรวจโควต้า Routes API | ปริมาณมากอาจเจอ 429 / หมดโควต้า |
| 9 | `duration` ถูกขอแต่ไม่ใช้ | เสีย field mask โดยไม่บันทึก |
| 10 | encodeURI ทั้ง URL | ปกติใช้ได้ แต่พิกัดที่มีอักขระพิเศษควร encode ทีละส่วนจะชัวร์กว่า |

---

## 8) ความสัมพันธ์กับระบบ Phaopanya Master

| ระบบ | หน้าที่ |
|------|---------|
| **Phaopanya Master (ปุ่ม 1/2/3)** | สร้าง/แมชต์สถานที่ → ได้ LAT/LNG, ระยะทางจากคลัง, ที่อยู่ราชการ |
| **สคริปต์นี้** | เอาพิกัดหลายจุดใน 1 Shipment ไปจัดลำดับ + ยิง Routes API → ได้ระยะทางเส้นทางจริงสำหรับเบิก |

เชื่อมกันที่ข้อมูล:
- ชีต `SCGนครหลวงJWDภูมิภาค` เป็น SOURCE ร่วม
- คอลัมน์ `ระยะทางจากคลัง_Km` / พิกัดปลายทาง มักมาจาก pipeline geo/master ก่อนหน้า
- สคริปต์นี้ **ไม่อ่าน MASTER_PLACE / SYS_TH_GEO** โดยตรง

---

## 9) สรุปสั้น ๆ แบบ “ระบบคิดอย่างไร”

1. รับ Shipment หนึ่งใบ  
2. เก็บทุกจุดส่งของใบนั้น + ปักคลังวังน้อยเป็นจุดเริ่ม  
3. ใช้ระยะจากคลังที่มีอยู่ เลือก “จุดไกลสุด” เป็นที่จบเส้นทาง  
4. จุดกลางให้ Google จัดใหม่ (ถ้ามีมากกว่า 1)  
5. วัดระยะทางแบบไม่สนรถติด  
6. เขียนลำดับพิกัด + กม. + ลิงก์ กลับชีตเบิก  

---

## 10) คำถามที่ควรตอบต่อ ถ้าจะใช้จริง/ปรับปรุง

1. ต้องการระยะ **ขาไปอย่างเดียว** หรือ **ไป–กลับคลัง**  
2. `ระยะทางจากคลัง_Km` ใน SOURCE มาจากสูตร/สคริปต์ไหน และอัปเดตล่าสุดเมื่อไหร่  
3. มี Shipment ที่จุดส่ง > 20 จุดบ่อยไหม  
4. ใครเป็นคนเรียก `findOptimalRouteUsingExistingDistance` (AppSheet / ปุ่ม / library)  
5. ต้องการรวมเข้าเมนู Phaopanya หรือแยกโปรเจกต์ไว้คนละ Apps Script  

ถ้าต้องการ ขั้นถัดไปช่วยได้ เช่น:
- วาด sequence diagram แบบละเอียด  
- ชี้จุดที่ควร soft-fail แทน throw  
- ออกแบบเวอร์ชัน “ไป–กลับคลัง”  
- เทียบกับข้อมูลจริงในชีต `SCGนครหลวงJWDภูมิภาค`

## รายงานตรวจโค้ด Routing PRO — ประสิทธิภาพ / Error เงียบ / ซ้ำซ้อน / จุดควรแก้ / API ใหม่

สรุปสั้นก่อน: **โครงสร้างหลักใช้ถูกทางแล้ว** (Routes API v2 + TextFinder + TRAFFIC_UNAWARE)  
แต่ยังมี **error เงียบหลายจุด**, **I/O ชีตซ้ำในลูป**, และ **ขอบเขต API ที่ยังไม่ได้กัน**

---

## 1) ประสิทธิภาพ — ดีแค่ไหน

| จุด | คะแนน | รายละเอียด |
|-----|--------|------------|
| ค้นหา Shipment ด้วย TextFinder เฉพาะคอลัมน์ | ดีมาก | ไม่โหลดทั้งชีต — เหมาะชีตใหญ่ |
| หาแถว RESULT ด้วย TextFinder | ดี | ดีกว่า `getValues()` ทั้งคอลัมน์เมื่อแถวเยอะ |
| เขียนปลายทาง 01–20 แบบ batch 1 ครั้ง | ดี | ลดครั้งเรียก Sheets |
| เขียนผลหลัก 3 คอลัมน์ทีละ `setValue` | ปานกลาง | รวมเป็น 1 ช่วงได้ถ้าระยะคอลัมน์ใกล้กัน |
| อ่านพิกัด/ชื่อ/ระยะ **ทีละแถวในลูป** | อ่อน | N จุด = 2N–3N ครั้ง `getRange().getValue()` |
| Field mask แคบ | ดี | ตรงแนวทาง Google (ไม่ใช้ `*`) |
| `TRAFFIC_UNAWARE` | ดีสำหรับเบิก | latency ต่ำ ระยะนิ่ง |
| ไม่มี cache ผลซ้ำ | อ่อน | กด shipment เดิมซ้ำ = ยิง API ใหม่ทุกครั้ง |

**คอขวดจริง:** ไม่ใช่ API แต่เป็น **การอ่านเซลล์ทีละแถว** หลัง TextFinder

ตัวอย่าง: Shipment มี 15 จุด → อย่างน้อย ~30–45 ครั้งอ่านเซลล์  
ถ้าโหลดเฉพาะแถวที่ match เป็น block (หรืออ่าน 3 คอลัมน์ทีเดียวต่อแถว) จะเร็วขึ้นชัด

**สรุปประสิทธิภาพ:** ดีกว่ารุ่นอ่านทั้งชีตมาก แต่ยังไม่ถึงระดับ “batch-read หลังค้นหา”

---

## 2) Error เงียบ (Silent failures) — พบหลายจุด

นี่คือส่วนที่ควรแก้ก่อนขยายใช้งาน

| # | จุดในโค้ด | พฤติกรรมตอนนี้ | ทำไมอันตราย |
|---|-----------|----------------|-------------|
| 1 | พิกัดว่าง / parse ไม่ได้ | `return` ข้ามแถว ไม่นับ ไม่ log | Shipment เหลือจุดน้อยกว่าจริง แต่ยังคำนวณต่อได้ → ระยะทาง belittled |
| 2 | `ระยะทางจากคลัง_Km` ว่าง | ใช้ `0` | จุดนั้นอาจกลายเป็น “ใกล้สุด” ผิด → เลือกปลายทางสุดท้ายผิดทั้งใบ |
| 3 | คอลัมน์ผลลัพธ์บางตัวไม่มี | `if (col !== -1)` ข้ามเงียบ | คิดว่าสำเร็จ แต่ระยะ/ลิงก์ไม่ถูกเขียน |
| 4 | `matches` ว่าง (นอกจาก ≤1 หลังรวม depot) | throw ทีหลัง | โอเค แต่ไม่มีสถิติว่าข้ามกี่จุดเพราะพิกัดเสีย |
| 5 | API คืน `routes` ว่าง | อ่าน `result.routes[0]` ตรง ๆ | เสี่ยง TypeError ไม่ชัดเจน |
| 6 | จุดลูกค้า > 20 | ตัดในคอลัมน์แยก เงียบ | string หลักครบ แต่ช่อง 01–20 ไม่ครบ — ผู้ใช้ไม่รู้ |
| 7 | `duration` ถูกขอใน field mask แต่ไม่ใช้ | ไม่ error | ไม่ใช่ silent bug แต่เสีย response ที่ไม่จำเป็น (เล็กน้อย) |
| 8 | พิกัดรูปแบบ `lat lng` (เว้นวรรค ไม่มีคอมมา) | ถูก strip แล้วไม่เข้าเงื่อนไข `,` | ข้ามจุดเงียบ |

**ระดับความรุนแรง**
- สูง: #1, #2 (ทำให้ตัวเลขเบิกผิดโดยไม่แจ้ง)
- กลาง: #3, #5, #6
- ต่ำ: #7, #8

**แนะนำขั้นต่ำ:** เก็บ `skipped[]` + `warnings[]` แล้วคืน/ log / เขียนคอลัมน์สถานะ เช่น `Routing_Status`, `Routing_Warning`

---

## 3) ฟังก์ชันซ้ำซ้อน / โครงสร้าง

| รายการ | สถานะ |
|--------|--------|
| 5 ฟังก์ชันหลักแยกหน้าที่ | ไม่ซ้ำซ้อน — ดี |
| Logic หา header index | ซ้ำ 2 ที่ (SOURCE + RESULT) | ควรเป็น helper `indexHeader_(sheet, names)` |
| TextFinder หา key | ซ้ำแพทเทิร์น | ทำ `findRowByValue_(sheet, colIdx, value)` ได้ |
| สร้าง object จุด (`original` + `forApi`) | ไม่ซ้ำ แต่แปลงพิกัดกระจาย | รวม `parseLatLng_(raw)` กลางที่เดียว |
| ไม่มีฟังก์ชันซ้ำชื่อ/ทำอย่างเดียวกันทั้งก้อน | ผ่าน | |

**สรุป:** ไม่มี dead code ชัด แต่มี **pattern ซ้ำ** ที่ควรถอดเป็น utility 3–4 ตัว

---

## 4) จุดควรปรับปรุง / เพิ่ม / แก้ (เรียงความสำคัญ)

### P0 — แก้ก่อนใช้จริงหนัก ๆ

1. **นับและรายงานจุดที่ข้าม**  
   พิกัดเสีย / ไม่มีระยะจากคลัง → ต้องรู้จำนวน
2. **กัน `routes[0]` ว่าง**  
   ตรวจ `result.routes && result.routes.length` ก่อนใช้
3. **อย่าใช้ `distance \|\| 0` กับจุดที่ไม่มีระยะ**  
   แยก `hasDepotDistance` — ถ้าไม่มีครบ ควร fail หรือ fallback ชัด (เช่น เรียงตามลำดับเดิม)
4. **สถานะเขียนกลับชีต**  
   `OK` / `OK_WITH_WARNINGS` / `FAILED` + ข้อความสั้น

### P1 — ประสิทธิภาพและความทนทาน

5. **Batch อ่านหลัง TextFinder**  
   เก็บ `rowIdx` ทั้งหมด → หา min–max หรืออ่านทีละแถวแบบ `getRange(row, latCol, 1, 3)` รวมพิกัด+ชื่อ+ระยะ
6. **LockService** ตอนเขียน RESULT  
   กัน race ถ้ารันหลาย shipment พร้อมกัน
7. **Retry เบา ๆ เมื่อ API 429/503**  
   1–2 ครั้ง + sleep
8. **ตรวจจำนวน intermediates**  
   Routes API รองรับ intermediate ประมาณ 25 จุด — ตอนนี้ไม่กันบน
9. **Timeout header** เมื่อ optimize  
   Google แนะนำ `X-Server-Timeout` ≥ 10s สำหรับ optimize waypoints

### P2 — คุณภาพข้อมูลเบิก

10. **โหมดไป–กลับคลัง (optional)**  
    ตอนนี้ one-way จบจุดไกลสุด — ถ้านโยบายเบิกคิดขากลับ ต้องมี flag
11. **บันทึกจำนวนจุดที่ใช้จริง / จุดที่ข้าม**
12. **อย่า hard-code `SPREADSHEET_ID`**  
    ใช้ `SpreadsheetApp.getActiveSpreadsheet()` หรือ Config กลาง
13. **แคชผลตาม `shipmentId + hash พิกัด`**  
    ลดยิง API ซ้ำ

### P3 — ของมีแล้วแต่ยังไม่ใช้ประโยชน์

14. เอา `routes.duration` ออกจาก field mask ถ้าไม่เขียน  
    หรือเก็บเป็น `ระยะเวลา_นาที` ถ้าต้องการ
15. ลิงก์แผนที่: encode ทีละ segment แทน `encodeURI` ทั้งก้อน (ชัวร์กว่าในเคสพิเศษ)

---

## 5) Google Maps API — ของใหม่ / ของที่ควรรู้

โค้ดใช้ **Routes API (`computeRoutes`)** อยู่แล้ว — **ถูกทาง**  
Directions API (Legacy) ถูกจัดเป็น Legacy แล้ว ไม่ควรย้อนกลับ

| ความสามารถ | ควรใช้กับงานนี้ไหม | เหตุผล |
|------------|---------------------|--------|
| `optimizeWaypointOrder` | ใช้อยู่แล้ว | ถูกต้องเมื่อ intermediate > 1 |
| `TRAFFIC_UNAWARE` | ใช้อยู่แล้ว | เหมาะใบเบิกที่ต้องการตัวเลขนิ่ง |
| `TRAFFIC_AWARE` / `TRAFFIC_AWARE_OPTIMAL` | ไม่จำเป็นสำหรับเบิกมาตรฐาน | ตัวเลขผันตามเวลา; ใช้แยกเป็นโหมด “ประมาณเวลาจริง” ได้ |
| Field mask เฉพาะ | ใช้อยู่แล้ว | ถูกแนวทาง Google |
| `Compute Route Matrix` | ยังไม่จำเป็น | ใช้เมื่ออยากได้ระยะทุกคู่จุดเอง (เช่น pre-score) ก่อน optimize |
| Toll (`travelAdvisory.tollInfo`) | Optional | ถ้าอนาคตคิดค่าผ่านทาง |
| Vehicle/trailer constraints | ไม่จำเป็นตอนนี้ | งานรถทั่วไป |
| Two-wheeler mode | ไม่เกี่ยว | |
| Polyline ใน response | Optional | ถ้าจะวาดเส้นในชีต/แดชบอร์ด |
| Routes Preferred / advanced optimization | ไม่เร่งด่วน | ซับซ้อน ราคา/สิทธิ์ต่างชุด |

**ข้อจำกัดที่ต้องใส่ในโค้ด**
- Intermediate waypoints โดยทั่วไปสูงสุด ~25  
- Optimize ใช้ได้กับ stopover เท่านั้น (ห้าม via)
- ขอ `optimizedIntermediateWaypointIndex` ใน field mask ทุกครั้งที่ `optimizeWaypointOrder: true` — โค้ดทำถูกแล้ว

**สิ่งที่ไม่ต้องเปลี่ยน**
- ไม่ต้องย้ายกลับไป Directions API เดิม  
- `TRAFFIC_UNAWARE` สำหรับเบิกเงินยังเป็นทางเลือกที่เหมาะสม

---

## 6) ตารางตัดสินใจ “ควรทำอะไร”

| หัวข้อ | ผ่าน/ไม่ผ่าน | แนะนำ |
|--------|----------------|--------|
| ใช้ API รุ่นใหม่ | ผ่าน | คง Routes API |
| โครงสร้างฟังก์ชัน | ผ่าน | แยกชั้นชัด |
| ประสิทธิภาพอ่านชีต | ไม่ผ่านเต็ม | batch หลัง TextFinder |
| Silent skip พิกัด/ระยะ | ไม่ผ่าน | ต้องมี warning + นับ |
| กัน race ตอนเขียน | ไม่ผ่าน | LockService |
| กัน API routes ว่าง | ไม่ผ่าน | null-check |
| ซ้ำซ้อนฟังก์ชัน | ผ่านเกือบ | ถอด helper header/finder/parse |
| ฟีเจอร์ API ใหม่ที่ขาดแล้วพัง | ไม่มี | ไม่มี blocker |
| พร้อม production หนัก | เงื่อนไข | หลังแก้ P0 |

---

## 7) สรุปเป็นภาษางาน

**ของดีอยู่แล้ว**
- TextFinder ไม่กวาดทั้งชีต  
- Routes API v2 + optimize + TRAFFIC_UNAWARE  
- เขียน RESULT แบบไม่ทับทั้งแถว  
- Field mask ไม่ฟุ่มเฟือย  

**ของที่ยังเสี่ยง**
- ข้ามจุดเสียเงียบ → ระยะทางเบิก belittled ได้  
- ระยะจากคลังเป็น 0 เมื่อข้อมูลว่าง → ลำดับปลายทางพลิก  
- อ่านเซลล์ทีละจุดในลูป → ช้าเมื่อจุดเยอะ  
- ไม่มี lock / retry / สถานะเขียนกลับ  

**API ใหม่:** ไม่มีตัวที่ “ต้องย้ายทันที”  
ของที่ควรเพิ่มเป็นขั้นถัดไปคือ **ความปลอดภัยของข้อมูล + สถานะ + batch read** มากกว่าเปลี่ยน endpoint

---

ถ้าต้องการให้ลงมือต่อ แนะนำทำเป็นแพตช์สั้น 3 จุดก่อน:
1. `parseLatLng_` + นับ skipped  
2. กัน `routes[0]` ว่าง + สถานะเขียนชีต  
3. batch อ่านค่าต่อแถวหลัง TextFinder  

บอกได้ว่าจะให้แก้เป็นไฟล์ `.gs` ส่งกลับเลย หรือทำ checklist ตรวจบนชีตจริงก่อน