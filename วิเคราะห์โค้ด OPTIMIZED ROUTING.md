## วิเคราะห์โค้ด: OPTIMIZED ROUTING SCRIPT (PRO VERSION)

วิเคราะห์จากไฟล์ที่ส่งมาทั้งก้อน — **ยังไม่ได้รันจริง / ยังไม่ได้ยิง API จริง** จึงพูดได้ระดับ **static analysis** เท่านั้น

---

## 1) โค้ดนี้ทำอะไรโดยรวม

เป็นสคริปต์คำนวณเส้นทางรถส่งสินค้าเพื่องาน **เบิกส่วนต่าง SCG วังน้อย**

```text
Shipment ID
   → หาทุกจุดส่งในชีต SCGนครหลวงJWDภูมิภาค
   → ใส่คลังวังน้อยเป็นจุดเริ่ม
   → เรียงจุดโดยใช้ "ระยะทางจากคลัง" (ไกลสุด = ปลายทาง)
   → เรียก Google Routes API (แบบไม่คิดจราจร)
   → เขียนผลลงชีต ทำเบิกส่วนต่างScgวังน้อย
```

จุดประสงค์หลักไม่ใช่ “เส้นทางสวยที่สุดตลอดเวลา” แต่เป็น **เส้นทางมาตรฐานที่คำนวณซ้ำแล้วได้ผลใกล้เคียงกัน** สำหรับใบเบิก

---

## 2) โครงไฟล์ (5 ส่วน)

| ส่วน | หน้าที่ |
|------|---------|
| 1. CONFIG | ชีต, คลัง, ชื่อคอลัมน์ผลลัพธ์ |
| 2. MAIN | `findOptimalRouteUsingExistingDistance` — จุดเข้าหลัก |
| 3. DATA PREP | หา waypoints + เรียงลำดับ |
| 4. API | ยิง Google Routes API |
| 5. WRITE | เขียนผลกลับชีตผลลัพธ์ |

---

## 3) Configuration ที่ล็อกไว้

| ค่า | ความหมาย |
|-----|----------|
| `SPREADSHEET_ID` | ชีตงานตายตัว (hard-code) |
| `SHEET_COMPUTED` | `SCGนครหลวงJWDภูมิภาค` — อ่านจุดส่ง |
| `SHEET_RESULT` | `ทำเบิกส่วนต่างScgวังน้อย` — เขียนผล |
| `DEPOT_COORDS` | คลังวังน้อย `14.1646106, 100.6254644` |
| `RESULT_COLUMN_NAME` | `GoogleMapsRoutesAPI` — สตริงพิกัดเรียงแล้ว |
| `DISTANCE_COLUMN_NAME` | `ระยะทาง_GoogleMapAPI_Km` |
| `LINK_COLUMN_NAME` | ลิงก์ Google Maps |
| `DEPOT_DISTANCE_COLUMN` | `ระยะทางจากคลัง_Km` — ใช้เรียงจุด |

**หมายเหตุ:** API Key **ไม่ได้ฝังในโค้ด** อ่านจาก `PropertiesService` ชื่อ `GOOGLE_MAPS_API_KEY` — ดีกว่า hard-code

---

## 4) Flow ทีละขั้น (ละเอียด)

### ขั้น A — ฟังก์ชันหลัก

`findOptimalRouteUsingExistingDistance(shipmentId, rowId)`

1. เปิดสเปรดชีตตาม ID  
2. เตรียมรายการจุด (`prepareWaypointsWithExistingDistance`)  
3. ถ้าจุด ≤ 1 → error (มีแค่คลัง หรือไม่มีจุดส่ง)  
4. เรียงลำดับ (`selectFinalDestinationAndSort`)  
5. ยิง API (`executeGoogleMapsRoutesAPIOneWay`)  
6. เขียนผล (`writeResultsToSheet`) ด้วย `rowId`  
7. คืน `{ Status, CalculatedDistanceKm, GoogleMapsLink }`

มักถูกเรียกจาก **AppSheet / Bot** ส่ง `shipmentId` + `rowId` เข้ามา

---

### ขั้น B — เตรียมจุดส่ง

`prepareWaypointsWithExistingDistance(ss, shipmentId)`

**อ่านอะไร**
- Header แถว 1 ของ `SCGนครหลวงJWDภูมิภาค`
- หาคอลัมน์:
  - `Shipment No`
  - `จุดส่งสินค้าปลายทาง`
  - `ชื่อปลายทาง`
  - `ระยะทางจากคลัง_Km`

**วิธีหาแถว**
- ใช้ `TextFinder` เฉพาะคอลัมน์ `Shipment No`
- `matchEntireCell(true)` → ต้องตรงทั้งเซลล์
- ได้เฉพาะแถวของ shipment นั้น

**สร้างรายการจุด**
1. **จุด 0 เสมอ = คลัง (Depot)**  
2. ทุกแถวที่เจอ:
   - อ่าน `จุดส่งสินค้าปลายทาง`
   - ถ้าว่าง → ข้าม
   - ล้างตัวอักษรแปลก เหลือแค่ตัวเลข / จุด / จุลภาค / ลบ
   - แยก `lat,lng`
   - อ่านระยะจากคลัง (ถ้ามีคอลัมน์)
   - อ่านชื่อปลายทาง (ถ้าไม่มีใช้ `Point N`)

**ผลลัพธ์**
```text
[ Depot, จุดส่ง1, จุดส่ง2, ... ]
```

---

### ขั้น C — เรียงลำดับก่อนยิง API

`selectFinalDestinationAndSort(allPoints)`

กติกา:
1. แยก depot ออก  
2. เรียงจุดส่งตาม `distance` **มาก → น้อย**  
3. **จุดที่ไกลคลังที่สุด = ปลายทางสุดท้าย**  
4. จุดที่เหลือ = จุดแวะกลางทาง  
5. ลำดับส่งเข้า API:

```text
[ คลัง ] → [ จุดกลาง... ] → [ จุดไกลสุด ]
```

**สำคัญ:**  
ตอนนี้ยัง **ไม่ได้** ให้ Google เลือกลำดับทั้งหมดจากศูนย์  
แต่ **ล็อกปลายทาง** ด้วยระยะจากคลังที่มีอยู่แล้วในชีต  
แล้วค่อยให้ API optimize เฉพาะจุดกลาง (ถ้ามีมากกว่า 1)

---

### ขั้น D — เรียก Google Routes API

`executeGoogleMapsRoutesAPIOneWay(orderedPoints)`

| รายการ | ค่า |
|--------|-----|
| Endpoint | `https://routes.googleapis.com/directions/v2:computeRoutes` |
| Method | POST |
| Auth | Header `X-Goog-Api-Key` |
| `travelMode` | `DRIVE` |
| `routingPreference` | **`TRAFFIC_UNAWARE`** |
| `optimizeWaypointOrder` | เปิดเมื่อจุดกลาง > 1 |

ทำไมใช้ `TRAFFIC_UNAWARE`  
→ ระยะทางไม่ผันตามชั่วโมงจราจร  
→ เหมาะงานเบิกที่ต้องการตัวเลข **นิ่ง ทำซ้ำได้**

**FieldMask ที่ขอ**
- `routes.duration`
- `routes.distanceMeters`
- `routes.optimizedIntermediateWaypointIndex`

**หลังได้ผล**
1. ระยะรวม = `distanceMeters / 1000` (กม.)  
2. ถ้า API ส่งลำดับจุดกลางที่ optimize แล้ว → เรียงตามนั้น  
3. ถ้าไม่ → ใช้ลำดับเดิม  
4. สร้างลิงก์:

```text
https://www.google.com/maps/dir/lat,lng/lat,lng/...
```

แล้ว `encodeURI`

---

### ขั้น E — เขียนผลลงชีต

`writeResultsToSheet(resultSheet, rowId, ...)`

ชีต: `ทำเบิกส่วนต่างScgวังน้อย`

**หาแถวด้วย**
- คอลัมน์ `ID_ทำเบิกส่วนต่างScgวังน้อย`
- TextFinder หา `rowId`

**เขียนอะไร**

| คอลัมน์ | ค่า |
|---------|-----|
| `GoogleMapsRoutesAPI` | `lat, lng \| lat, lng \| ...` |
| `ระยะทาง_GoogleMapAPI_Km` | ตัวเลขกม. |
| `แสดงแผนที่_GoogleMapsRoutesAPI` | URL แผนที่ |
| `Lat/Long_ปลายทาง_01` … `_20` | พิกัดลูกค้าทีละช่อง (ไม่รวมคลัง) สูงสุด 20 จุด |

รูปแบบสตริงหลัก:
```text
14.164611, 100.625464 | 13.75..., 100.55... | ...
```
(มีช่องว่างหลังคอมมา, คั่นด้วย ` | `)

---

## 5) แผนภาพ Control Flow

```text
findOptimalRouteUsingExistingDistance(shipmentId, rowId)
        │
        ▼
prepareWaypointsWithExistingDistance
        │  TextFinder: Shipment No
        │  อ่าน lat/lng, ชื่อ, ระยะจากคลัง
        │  ใส่ Depot นำหน้า
        ▼
selectFinalDestinationAndSort
        │  เรียงระยะจากคลังมาก→น้อย
        │  ไกลสุด = destination
        ▼
executeGoogleMapsRoutesAPIOneWay
        │  Routes API + TRAFFIC_UNAWARE
        │  optimize จุดกลาง (ถ้า >1)
        │  สร้าง maps link
        ▼
writeResultsToSheet
        │  หา row ด้วย ID ใบเบิก
        │  เขียน string / km / link / 01-20
        ▼
return Success + ระยะ + link
```

---

## 6) จุดแข็ง

1. **API Key อยู่ใน Script Properties** ไม่โชว์ในซอร์ส  
2. **TextFinder** หาเฉพาะคอลัมน์ shipment — เร็วกว่าโหลดทั้งชีตเมื่อข้อมูลใหญ่  
3. **`TRAFFIC_UNAWARE`** เหมาะงานเบิกที่ต้องตัวเลขนิ่ง  
4. **Depot ถูกล็อกเป็นจุดเริ่มเสมอ**  
5. ใช้ระยะจากคลังที่มีอยู่แล้วมาช่วยจัดลำดับ — ลดการเดาล้วน ๆ  
6. เขียน `Lat/Long_01..20` แบบ batch แถวเดียว  
7. ทำความสะอาด lat/lng ก่อน parse  

---

## 7) จุดเสี่ยง / ข้อจำกัด (จากโค้ดจริง)

### สูง

| เรื่อง | รายละเอียด |
|--------|------------|
| Hard-code Spreadsheet ID | ย้ายไฟล์/คัดลอกชีตแล้วชี้ผิดได้ |
| อ่านทีละเซลล์ในลูป TextFinder | `getRange(row, col).getValue()` ต่อแถว — shipment จุดเยอะจะช้า |
| ปลายทาง = ไกลคลังสุด | ไม่เสมอไปว่าเป็นลำดับส่งจริงที่ดีที่สุดในทุกเคส |
| ไม่มี retry / backoff เมื่อ API error | ล้มแล้ว throw ทันที |
| ไม่มี lock | ถ้ารันคู่ขนานเขียนแถวเดียวกัน อาจแข่งกัน |

### กลาง

| เรื่อง | รายละเอียด |
|--------|------------|
| สูงสุด 20 จุดในคอลัมน์แยก | จุดที่ 21+ อยู่ใน string/link แต่ไม่เข้าช่อง 01–20 |
| `optimizeWaypointOrder` เฉพาะจุดกลาง | ต้นทาง/ปลายทางถูกล็อกแล้ว |
| ชื่อคอลัมน์ต้องตรงเป๊ะ | เปลี่ยนหัวตารางแล้วหาไม่เจอ |
| ถ้า `ระยะทางจากคลัง_Km` ว่าง/0 ทั้งชุด | การเรียงเกือบสุ่มตามลำดับเดิม |

### ต่ำ / ข้อสังเกต

| เรื่อง | รายละเอียด |
|--------|------------|
| `encodeURI` ทั้ง URL | ใช้ได้ แต่พิกัดอย่างเดียวมักไม่จำเป็นมาก |
| Optional chaining ในคอมเมนต์ | ในไฟล์นี้แทบไม่ได้ใช้จริง |
| ระยะในผลเป็น number | ถ้าชีตมีรูปแบบเซลล์ผิด อาจแสดงแปลก |

---

## 8) ตัวอย่าง logic ด้วยตัวเลขสมมติ

Shipment มี 3 จุด:

| จุด | ระยะจากคลัง |
|-----|-------------|
| A | 12 กม. |
| B | 45 กม. |
| C | 30 กม. |

หลัง sort:
- ปลายทาง = **B** (ไกลสุด)
- จุดกลาง = A, C (อาจถูก API สลับลำดับ)

ลำดับเข้า API เริ่มต้น:
```text
Depot → A → C → B
```
หรือ
```text
Depot → C → A → B
```
ถ้า API optimize จุดกลาง

ระยะรวมที่ได้ = ระยะขับรถตามถนนทั้งทริป (ไม่ใช่แค่ผลรวมระยะจากคลัง)

---

## 9) ความสัมพันธ์กับระบบ Phaopanya / Master

| ระบบ Master | สคริปต์นี้ |
|-------------|-----------|
| สะสม MASTER, แมชต์ geo, POINTS | **ไม่เกี่ยวโดยตรง** |
| ชีต `SCGนครหลวงJWDภูมิภาค` | **อ่านร่วม** (พิกัด, ชื่อ, ระยะจากคลัง) |
| ปุ่ม 1/2/3 | ไม่เรียกสคริปต์นี้ |
| ผลลัพธ์ | ชีต **ทำเบิกส่วนต่างScgวังน้อย** |

พึ่งพาข้อมูลที่ Master/Maps เตรียมไว้ก่อนแล้ว โดยเฉพาะ:
- `จุดส่งสินค้าปลายทาง` ต้องมี
- `ระยะทางจากคลัง_Km` ยิ่งมี ยิ่งเรียงได้ดี

---

## 10) สิ่งที่โค้ด **ไม่ได้ทำ**

- ไม่คำนวณจากจราจรจริง (`TRAFFIC_AWARE`)  
- ไม่วนกลับคลัง (one-way ไม่ใช่ round trip)  
- ไม่แก้พิกัดที่ผิด  
- ไม่สร้าง shipment ใหม่  
- ไม่แตะ MASTER_PLACE / SYS_TH_GEO  
- ไม่ validate ว่าพิกัดอยู่ในไทย  
- ไม่มี unit test ในไฟล์นี้  

---

## 11) สรุปแบบตรง ๆ

| หัวข้อ | สรุป |
|--------|------|
| หน้าที่หลัก | คำนวณเส้นทางคลัง → จุดส่ง ตาม shipment แล้วเขียนระยะ+ลิงก์ลงใบเบิก |
| จุดเข้า | `findOptimalRouteUsingExistingDistance(shipmentId, rowId)` |
| ข้อมูลเข้า | แถวใน `SCGนครหลวงJWDภูมิภาค` ที่ Shipment No ตรงกัน |
| หัวใจการเรียง | ไกลคลังสุด = ปลายทาง, จุดอื่นเป็นทางผ่าน (API ช่วยจัดจุดกลางได้) |
| API | Google Routes v2, ขับรถ, ไม่คิดจราจร |
| จุดแข็ง | Key ไม่ฝังโค้ด, TextFinder, มาตรฐานเบิกนิ่ง, batch เขียน 20 ช่อง |
| จุดอ่อนหลัก | อ่านทีละเซลล์, hard-code sheet id, ไม่มี retry/lock, พึ่งระยะจากคลังที่มีอยู่ |

---

ถ้าต้องการต่อโดยยังไม่แก้โค้ด เลือกได้ว่าจะให้เจาะข้อไหนต่อ:

1. **ความเสี่ยงตอนรันจริง** (โควต้า, เวลา, AppSheet เรียกยังไง)  
2. **เทียบกับ skill/แนวทาง route claim วังน้อย** ว่าตรงหรือต่างตรงไหน  
3. **แผนปรับปรุง** แบบไม่เปลี่ยนพฤติกรรมใบเบิก (เช่น ลด `getValue` ทีละเซลล์)

## สรุปภาพรวม

โค้ดชุดนี้ **ใช้งานได้จริงและทิศทางถูกต้อง** สำหรับงานเบิกส่วนต่าง (ระยะนิ่ง + ลำดับมาตรฐาน)  
แต่ยังมีช่องโหว่ด้าน **ประสิทธิภาพการอ่านชีต**, **error เงียบบางจุด**, และ **ขอบเขต API** ที่ควรรู้ก่อนใช้หนัก

ระดับความพร้อมโดยประมาณ:

| ด้าน | คะแนน | ความหมาย |
|------|--------|----------|
| ตรรกะธุรกิจ (เบิกเงิน) | ดี | `TRAFFIC_UNAWARE` + ล็อกปลายทางไกลสุด เหมาะงานเบิก |
| ความถูกต้อง API | ดี | ใช้ Routes API รุ่นปัจจุบันถูกทาง |
| ประสิทธิภาพชีต | ปานกลาง | TextFinder ดี แต่ยัง `getValue()` ทีละเซลล์ |
| ความทนทาน (error/retry) | อ่อน | ล้มแล้ว throw, มีจุดกลืนค่าเงียบ |
| ความปลอดภัย | ดี | API Key อยู่ใน Script Properties |

---

## 1) ประสิทธิภาพ — ดีตรงไหน / ช้าตรงไหน

### สิ่งที่ทำได้ดี

1. **TextFinder เฉพาะคอลัมน์ `Shipment No`**  
   ไม่ต้องโหลดทั้งชีตมา filter ในหน่วยความจำ — ถูกทางเมื่อชีตใหญ่

2. **เขียน `Lat/Long_01..20` แบบ batch ครั้งเดียว**  
   `setValues` แถวเดียว ดีกว่า set ทีละช่อง

3. **FieldMask แคบ**  
   ขอแค่ `duration, distanceMeters, optimizedIntermediateWaypointIndex`  
   ลดขนาด response และต้นทุนบางส่วน

4. **ไม่ optimize เมื่อจุดกลาง ≤ 1**  
   ลดงาน API ที่ไม่จำเป็น

### จุดที่กินเวลาจริง (สำคัญ)

ใน `prepareWaypointsWithExistingDistance` หลังหา `matches` แล้ว:

```text
แต่ละแถวที่เจอ
  → getValue() พิกัด
  → getValue() ระยะจากคลัง
  → getValue() ชื่อ
```

นี่คือ **อ่านทีละเซลล์**  
ถ้า shipment เดียวมี 15 จุด = ประมาณ 45 ครั้งเรียก Spreadsheet service  
GAS ช้าที่ I/O ชีตมากกว่าที่คำนวณ

**แนวเทียบ**

| วิธี | ลักษณะ |
|------|--------|
| ตอนนี้ | TextFinder + getValue ทีละช่อง |
| ดีกว่า | TextFinder ได้เลขแถว → อ่าน block `getValues()` ครั้งเดียวเฉพาะแถวที่ต้องใช้ หรืออ่านช่วงคอลัมน์ที่เกี่ยวแล้ว map ในหน่วยความจำ |

`writeResultsToSheet` ก็ `setValue` แยก 3 ช่อง (string / km / link)  
รวมกับ batch 20 ช่องได้ แต่ไม่ร้ายเท่าฝั่งอ่าน

### ประสิทธิภาพ API

- `TRAFFIC_UNAWARE` = เร็วและนิ่ง เหมาะเบิก  
- `optimizeWaypointOrder: true` เมื่อจุดกลาง > 1 จะช้าขึ้น  
  เอกสาร Google แนะนำ timeout สูงขึ้น (ประมาณ ≥ 10 วินาที) สำหรับ optimize  
- จุดคั่นกลางได้สูงสุด **25** ต่อ request (ต้นทาง/ปลายทางไม่นับในโควตานี้แบบเดียวกัน)  
  เกินนี้ต้องแยกทริป — โค้ดปัจจุบัน **ไม่มีกันพลาด**

---

## 2) Error เงียบ / พฤติกรรมที่พลาดได้โดยไม่รู้

| # | จุด | เกิดอะไร | ผลที่ผู้ใช้เห็น |
|---|-----|----------|----------------|
| 1 | พิกัดว่าง | `if (!latlngRaw) return` | ข้ามจุดนั้น **เงียบ** — เส้นทางขาดจุดโดยไม่มี log ชัดในชีต |
| 2 | พิกัด parse ไม่ได้ | หลัง clean แล้วไม่เข้าเงื่อนไข | ข้ามเงียบเหมือนกัน |
| 3 | `ระยะทางจากคลัง` ว่าง/ข้อความ | `parseFloat(...) \|\| 0` | ได้ 0 → เรียงผิดได้ (จุดสำคัญกลายเป็น “ใกล้สุด”) |
| 4 | ไม่มีคอลัมน์ระยะจากคลัง | ใช้ 0 ทั้งชุด | เรียงไม่มีความหมาย แต่ยังรันต่อ |
| 5 | คอลัมน์ผลบางตัวไม่พบ | `if (col !== -1) setValue` | เขียนบางช่อง บางช่องไม่เขียน — **ไม่ throw** |
| 6 | `routes[0]` สมมติว่ามีเสมอ | ถ้า API คืน routes ว่าง | จะพังตอนอ่าน `route.distanceMeters` (ไม่เงียบ แต่ข้อความอาจไม่เป็นมิตร) |
| 7 | พิกัดซ้ำหลายแถวใน shipment เดียว | ไม่ dedupe | ส่ง waypoint ซ้ำเข้า API ได้ |
| 8 | จุดกลาง > 25 | ไม่เช็ก | API error ทั้งทริป |
| 9 | `encodeURI` ทั้ง URL | โดยทั่วไปใช้ได้ | เคสพิเศษบางตัวอาจ encode เกินจำเป็น (ผลน้อย) |
| 10 | ไม่มีสถานะในชีตผล | สำเร็จ/ล้ม | ดูจาก AppSheet/log อย่างเดียว ไม่มีคอลัมน์ `RouteStatus` / เวลาคำนวณ |

**สรุป error เงียบที่อันตรายที่สุด**

1. ข้ามจุดที่พิกัดเสียโดยไม่รายงานจำนวนที่ข้าม  
2. ระยะจากคลัง = 0 ทำให้ลำดับปลายทางผิด แต่ระบบยัง “Success”  
3. เขียนผลไม่ครบเพราะชื่อคอลัมน์ไม่ตรง แต่ฟังก์ชันยังคืน Success

---

## 3) ฟังก์ชันซ้ำซ้อนไหม?

**ไม่พบฟังก์ชันซ้ำแบบ copy-paste คนละชื่อ**

โครงสร้างแบ่งชัด:

| ฟังก์ชัน | หน้าที่ |
|----------|---------|
| `findOptimalRouteUsingExistingDistance` | orchestrator |
| `prepareWaypointsWithExistingDistance` | อ่านจุด |
| `selectFinalDestinationAndSort` | เรียงลำดับ |
| `executeGoogleMapsRoutesAPIOneWay` | ยิง API |
| `writeResultsToSheet` | เขียนผล |

สิ่งที่ “คล้ายกัน” แต่ยังไม่ถึงขั้นซ้ำซ้อน:

- TextFinder หาแถว มีทั้งฝั่ง SOURCE และ RESULT → pattern ซ้ำได้ แต่คนละชีต ยังยอมรับได้  
- การอ่าน header ซ้ำทุกครั้งที่เรียก → ไม่ผิด แต่ cache header ได้ถ้าเรียกถี่

**ไม่มี dead function** ในไฟล์นี้

---

## 4) สิ่งที่ควรปรับปรุง (เรียงตามความคุ้ม)

### ควรทำ (ผลชัด / เสี่ยงลด)

1. **ลด `getValue` ทีละเซลล์**  
   หลังได้รายการเลขแถว ให้ batch อ่านค่า

2. **นับและรายงานจุดที่ข้าม**  
   เช่น `skippedInvalidLatLng: 2` ใน return / log / คอลัมน์สถานะ

3. **กันจุดกลางเกิน 25**  
   ถ้าเกิน → error ชัด หรือแยกคำนวณหลายท่อน (ถ้านโยบายธุรกิจยอม)

4. **Dedupe พิกัด** (หรือ dedupe ชื่อ+พิกัด) ใน shipment เดียวกัน

5. **ตรวจ `result.routes && result.routes.length`** ก่อนใช้ `routes[0]`

6. **คอลัมน์สถานะในชีตผล**  
   `RouteStatus`, `RouteError`, `CalculatedAt`, `WaypointCount`, `SkippedCount`

7. **Retry สั้น ๆ เมื่อ API 5xx / timeout**  
   1–2 ครั้ง พร้อม sleep

### พิจารณาทำ (ขึ้นกับงาน)

8. **อย่า hard-code Spreadsheet ID**  
   ใช้ `SpreadsheetApp.getActiveSpreadsheet()` ถ้าสคริปต์ผูกชีตอยู่แล้ว

9. **LockService** รอบเขียนแถวผล ถ้า AppSheet ยิงพร้อมกันได้

10. **รวม setValue 3 ช่องแรกเป็นช่วงเดียว** ถ้าคอลัมน์อยู่ติดกัน (ถ้าไม่ติดกัน แยกตามเดิมก็ได้)

11. **ถ้า `ระยะทางจากคลัง` ว่างทั้งชุด**  
    fallback: ใช้ระยะเส้นตรงจาก depot แทน 0 เพื่อให้เรียงยังมีเหตุผล

### ไม่ควรเปลี่ยนโดยไม่จำเป็น (เพื่อใบเบิก)

- `TRAFFIC_UNAWARE` → อย่าเปลี่ยนเป็น traffic-aware ถ้าต้องการตัวเลขซ้ำได้ทุกวัน  
- การล็อก “ไกลคลังสุด = ปลายทาง” → เป็นนโยบายธุรกิจ เปลี่ยนแล้วผลเบิกจะเทียบกับของเก่าไม่ได้ตรง ๆ

---

## 5) Google Routes API — ของใหม่ / ของที่มีแล้วที่ควรรู้

โค้ดใช้ **Routes API `computeRoutes`** ซึ่งเป็นทางที่ Google แนะนำแทน Directions API รุ่นเก่าอยู่แล้ว — **ทิศถูก**

### ของที่มีใน API และโค้ดใช้แล้ว

| ฟีเจอร์ | สถานะในโค้ด |
|---------|-------------|
| `computeRoutes` | ใช้แล้ว |
| `optimizeWaypointOrder` | ใช้แล้ว (จุดกลาง) |
| `TRAFFIC_UNAWARE` | ใช้แล้ว (เหมาะเบิก) |
| Field mask | ใช้แล้ว |

### ของที่มีใน API แต่โค้ดยังไม่ใช้ (ใช้เมื่อจำเป็นเท่านั้น)

| ฟีเจอร์ | เหมาะเมื่อ | ควรใช้กับงานเบิกไหม |
|---------|------------|---------------------|
| `TRAFFIC_AWARE` / `TRAFFIC_AWARE_OPTIMAL` | เวลาจริงบนถนน | **ไม่แนะนำ** สำหรับใบเบิกมาตรฐาน |
| `routeModifiers` (หลีกเลี่ยงทอลล์/ทางด่วน) | นโยบายรถบริษัท | ถ้ามีกฎรถ ถึงค่อยใส่ |
| Vehicle info / large vehicle | รถสูง หนัก ยาว | ถ้ารถใหญ่มีข้อจำกัดจริง |
| `computeRouteMatrix` | ตารางระยะหลายคู่จุด | ไม่แทนทริปเต็มเส้น แต่ช่วย precompute ระยะจากคลัง |
| Eco routing | ประหยัดน้ำมัน | ไม่เกี่ยวกับเบิกส่วนต่างโดยตรง |
| `via` waypoints (ผ่านโดยไม่จอด) | บังคับเส้นทาง | **ห้ามใช้คู่ optimize** (API จะ fail) |
| ขอ `routes.legs` ใน field mask | อยากได้ระยะทีละช่วง | มีประโยชน์ตอน audit |

### ข้อจำกัดที่ต้องจำ

- Intermediate waypoints สูงสุดประมาณ **25** ต่อ request  
- 11 จุดขึ้นไปคิดราคาสูงกว่าขั้นต่ำ  
- Optimize ต้องขอ field `optimizedIntermediateWaypointIndex` — โค้ดทำถูกแล้ว  
- Optimize ห้ามใช้กับ via waypoint และไม่เข้ากับ `TRAFFIC_AWARE_OPTIMAL`

**สรุปเรื่อง API ใหม่:**  
ไม่จำเป็นต้องย้ายไป API อื่นตอนนี้  
ควร **คง `computeRoutes` + `TRAFFIC_UNAWARE`**  
เพิ่มแค่การกัน limit, retry, และ leg details ถ้าต้องการตรวจสอบ

---

## 6) ความเสี่ยงธุรกิจเฉพาะงานเบิก

| ความเสี่ยง | คำอธิบาย |
|------------|----------|
| ลำดับพึ่ง `ระยะทางจากคลัง_Km` | ถ้าคอลัมน์นี้ยังไม่ถูกเติม/เก่า/ผิด เส้นทางจะเอนตามข้อมูลผิด |
| จุดพิกัดเสียถูกข้าม | ระยะรวมน้อยกว่าความเป็นจริง → เบิกคลาดได้ |
| ไม่มี audit trail | เถียงย้อนหลังยากว่าครั้งนั้นใช้กี่จุด ข้ามอะไร |
| AppSheet เรียกซ้ำ | ไม่มี idempotency key — คำนวณซ้ำทับค่าเดิม (โดยทั่วไปโอเค แต่ไม่มี log รอบ) |

---

## 7) ตารางตัดสินใจ: แก้อะไรก่อน

| ลำดับ | รายการ | เหตุผล | ความเร่ง |
|------|--------|--------|----------|
| 1 | รายงานจุดที่ข้าม + สถานะในชีต | กัน error เงียบกระทบยอดเบิก | สูง |
| 2 | Batch อ่านค่าแทน getValue ทีละช่อง | เร็วขึ้นชัดเมื่อจุดเยอะ | สูง |
| 3 | กัน >25 intermediates + ตรวจ routes ว่าง | กันพังทั้งทริป | สูง |
| 4 | Retry API ชั่วคราว | ลด fail จากเครือข่าย | กลาง |
| 5 | Dedupe พิกัด | กันจุดซ้ำ | กลาง |
| 6 | Fallback ระยะจากคลังด้วยเส้นตรง | เวลาคอลัมน์ระยะว่าง | กลาง |
| 7 | Lock ตอนเขียน | ถ้ามี concurrent | ต่ำ–กลาง |
| 8 | Traffic-aware / eco / truck | นอกนโยบายเบิกมาตรฐาน | ต่ำ (ไม่จำเป็น) |

---

## 8) คำตอบตรงคำถาม

| คำถาม | คำตอบสั้น |
|--------|-----------|
| มีประสิทธิภาพไหม? | **พอใช้** ฝั่ง API ดี ฝั่งอ่านชีตยังไม่โปรเมื่อจุดเยอะ |
| มี error เงียบไหม? | **มี** — ข้ามพิกัดเสีย, ระยะ 0, เขียนคอลัมน์ไม่ครบโดยไม่แจ้ง |
| ฟังก์ชันซ้ำซ้อนไหม? | **ไม่** โครงแยกชั้นดี |
| ควรปรับปรุงไหม? | **ควร** เฉพาะจุดทนทาน + อ่านชีต ไม่ต้องเปลี่ยนสถาปัตยกรรม |
| ต้องย้าย API ใหม่ไหม? | **ไม่ต้อง** — ใช้อยู่ถูกทางแล้ว เพิ่มการจัดการ limit/retry พอ |

---

## 9) สรุปสุดท้าย

โค้ดนี้เหมาะงาน **คำนวณเส้นทางมาตรฐานเพื่อเบิก** มากกว่าแอปนำทางสด  
จุดแข็งคือ Routes API ถูกตัว, โหมดจราจรนิ่ง, Key ไม่ฝังโค้ด, TextFinder, optimize จุดกลาง  
จุดอ่อนหลักคือ **อ่านชีตทีละเซลล์** และ **error เงียบตอนข้ามจุด/ระยะ 0** ซึ่งกระทบความน่าเชื่อถือของยอดระยะได้

ถ้าจะเดินต่อโดยยังไม่แก้โค้ดทันที แนะนำลำดับคิดดังนี้:

1. ตกลงว่า “ข้ามจุดพิกัดเสีย” ต้อง fail ทั้งใบ หรือทำต่อแล้วโชว์จำนวนข้าม  
2. ตกลงเพดานจุดต่อ shipment (≤ 25 หรือต้องแยกทริป)  
3. ค่อยลงมือแพตช์กลุ่มความทนทาน + batch read  

ถ้าต้องการขั้นถัดไป ผมช่วยแตกเป็น **แผนแพตช์ทีละจุดแบบไม่เปลี่ยนผลเบิกของเคสเดิม** ให้ได้ โดยยังไม่แตะโค้ดจนกว่าคุณจะสั่งให้แก้ครับ